import { style } from '@vanilla-extract/css';

export const separator = style({
  marginTop: 10,
});
export const height100 = style({
  height: '100%',
});

export const tabContainer = style({
  paddingTop: 20,
  height: '100%',
});
